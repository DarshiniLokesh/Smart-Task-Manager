import { Inngest } from "inngest";

// Initialize the Inngest client for Smart Task Manager
export const inngest = new Inngest({
  id: "smart-task-manager",
  name: "Smart Task Manager Background Jobs",
});
